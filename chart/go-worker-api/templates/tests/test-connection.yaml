apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-worker-api.fullname" . }}-test-connection"
  labels:
{{ include "go-worker-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "go-worker-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
